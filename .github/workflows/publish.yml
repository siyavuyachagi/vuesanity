# # .github/workflows/publish.yml
# name: Publish to npm

# on:
#   release:
#     types: [published]

# jobs:
#   publish:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout source
#         uses: actions/checkout@v4

#       - name: Setup Node
#         uses: actions/setup-node@v4
#         with:
#           node-version: 20
#           registry-url: https://registry.npmjs.org/

#       - name: Install dependencies
#         run: npm ci

#       - name: Build package
#         run: npm run build

#       - name: Publish package
#         run: npm publish
#         env:
#           NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}




# .github/workflows/publish.yml
name: Manual Release & Publish

on:
  release:
    types: [published]   # ONLY runs when YOU publish a GitHub Release

jobs:
  generate-changelog:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Run Release Please (for changelog only)
        uses: google-github-actions/release-please-action@v4
        with:
          release-type: node
          package-name: vuesanity
          command: manifest            # Prevents version bumps / tags
          monorepo-tags: false
          token: ${{ secrets.GITHUB_TOKEN }}

  publish:
    runs-on: ubuntu-latest
    needs: generate-changelog

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          registry-url: https://registry.npmjs.org/

      - name: Install dependencies
        run: npm ci

      - name: Build package
        run: npm run build

      - name: Publish package
        run: npm publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

