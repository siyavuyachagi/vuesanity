# # .github/workflows/publish.yml
# name: Publish to npm

# on:
#   release:
#     types: [published]

# jobs:
#   publish:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout source
#         uses: actions/checkout@v4

#       - name: Setup Node
#         uses: actions/setup-node@v4
#         with:
#           node-version: 20
#           registry-url: https://registry.npmjs.org/

#       - name: Install dependencies
#         run: npm ci

#       - name: Build package
#         run: npm run build

#       - name: Publish package
#         run: npm publish
#         env:
#           NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}


# .github/workflows/publish.yml
name: Release & Publish

on:
  push:
    branches:
      - main

jobs:
  release-please:
    runs-on: ubuntu-latest

    steps:
      - name: Release Please
        id: release
        uses: google-github-actions/release-please-action@v4
        with:
          release-type: node
          package-name: vuesanity

      # Only run publish if a new release was created
      - name: Checkout source
        if: steps.release.outputs.release_created == 'true'
        uses: actions/checkout@v4

      - name: Setup Node
        if: steps.release.outputs.release_created == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: 20
          registry-url: https://registry.npmjs.org/

      - name: Install dependencies
        if: steps.release.outputs.release_created == 'true'
        run: npm ci

      - name: Build package
        if: steps.release.outputs.release_created == 'true'
        run: npm run build

      - name: Publish package
        if: steps.release.outputs.release_created == 'true'
        run: npm publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
